# =============================================================================
# Environment Configuration for Todo Backend
# =============================================================================
# Copy this file to .env and fill in your actual values.
# NEVER commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration (Neon PostgreSQL)
# -----------------------------------------------------------------------------
# Format: postgresql+asyncpg://<user>:<password>@<endpoint>-pooler.<region>.neon.tech/<dbname>?sslmode=require
#
# Get your connection string from the Neon Console:
# 1. Go to https://console.neon.tech
# 2. Select your project
# 3. Click "Connection Details"
# 4. Enable "Connection pooling" toggle
# 5. Copy the connection string
#
# IMPORTANT:
# - Use the pooled connection string (with -pooler suffix) for better performance
# - SSL mode is required for all Neon connections
# - The asyncpg driver (postgresql+asyncpg://) is required for async operations
#
# Example:
# DATABASE_URL=postgresql+asyncpg://myuser:mypassword@ep-cool-name-123456-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require

DATABASE_URL=psql 'postgresql://neondb_owner:npg_XuBiEwy0sY4t@ep-winter-mouse-a19sttwc-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'

# -----------------------------------------------------------------------------
# Application Environment
# -----------------------------------------------------------------------------
# Set to "development" for verbose logging, "production" for optimized mode
# Default: development

ENVIRONMENT=development

# -----------------------------------------------------------------------------
# Server Configuration (Optional)
# -----------------------------------------------------------------------------
# HOST=0.0.0.0
# PORT=8000
